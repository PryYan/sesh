import React from 'react';
import { View, Text } from 'react-native';

const LoginScreen: React.FC = () => {
    return (
        <View>
            <Text>Login Screen</Text>
            {/* Add login components and logic */}
        </View>
    );
};

export default LoginScreen;
